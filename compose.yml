services:
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - b2hex
      - b2deci

  b2hex:
    build:
      context: ./b2hex

  b2deci:
    build:
      context: ./b2deci